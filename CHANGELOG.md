# Changelog

## 1.0.0 - 2026-09-19

### 新增
- 每轮通过临时用户上下文注入“短回复风格锚点”，避免长对话后模仿历史 assistant 的冗长文风。
- 普通闲聊最终回复硬长度兜底：按句子优先保留，并在自然标点处结束。
- 可配置普通闲聊软目标字数、最多句数与硬字数上限。
- 对“详细/分析/教程/代码/写作/报告”等实质任务自动跳过硬截短。
- 用户消息较长时自动跳过硬截短，减少误伤复杂问题。
- 可选清理常见括号动作描写，如“（点头）”“（挠头）”“（大拇指）”。
- `/style_guard_status` 状态查看命令。
- 调试日志与硬截短长度变化日志。

### 兼容
- 面向 AstrBot `>=4.24,<5`。
- 优先使用 `extra_user_content_parts + mark_as_temp()`；旧接口提供 `req.prompt` 兼容回退。
